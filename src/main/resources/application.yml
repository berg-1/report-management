spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB

  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/report?serverTimezone=GMT%2B8&characterEncoding=UTF-8  # 设置字符编码utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver  # tomcat 8.0+
    sql-script-encoding: UTF-8
server:
  tomcat:
    uri-encoding: UTF-8
debug: false